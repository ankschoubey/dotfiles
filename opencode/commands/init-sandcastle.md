Sandcastle is a TypeScript library for orchestrating AI coding agents in isolated sandboxes. This command replicates `npx sandcastle init`.

Follow the instructions in https://raw.githubusercontent.com/mattpocock/sandcastle/refs/heads/main/README.md

Note:

- To be able to run docker desktop you;d need to `source ~/.zshrc` then run `dctx desktop`
